# Organization Market-First + Live Route Fix

- Center panel uses `title` and `planDate` correctly.
- Center `Aç` action stores selected plan and opens `/organization/plans`.
- Publish action is market-first: created shift gets `roomId = null`.
- Organization map route continues to use company live-map stack, so route order / next stop / navigation features are available for organization operations after assignment.
